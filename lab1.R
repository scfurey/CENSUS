library(dplyr)
title <- c("Moby Dick", "Huck Finn", "Scarlet Letter")
author <- c("Herman Melville", "Mark Twain", "Nathaniel Hawthorne")
published <- c(1851, 1884, 1846) 
fake <- c("value 1", "value 2")

df <- data.frame(title, author, published)


%>%